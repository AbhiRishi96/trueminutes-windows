<Page
    x:Class="TrueMinutes.Windows.UI.Shell.AskPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    Background="{StaticResource BgApp}">

    <Grid Padding="32,24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Ask TrueMinutes"
                   Style="{StaticResource HeadingStyle}" Margin="0,0,0,4"/>
        <TextBlock Grid.Row="1"
                   Text="Search across your meetings. Try 'What did we decide about pricing?'"
                   Style="{StaticResource CaptionStyle}" Margin="0,0,0,16"/>

        <StackPanel Grid.Row="2" Spacing="12">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBox x:Name="QuestionBox" PlaceholderText="Ask a question across your meetings…"
                         Grid.Column="0" FontSize="14"/>
                <Button Grid.Column="1" Content="Ask"
                        Style="{StaticResource AccentButtonStyle}"
                        Margin="8,0,0,0" Click="OnAsk"/>
            </Grid>

            <ScrollViewer>
                <TextBlock x:Name="AnswerText" FontSize="14"
                           Foreground="{StaticResource TextPrimary}"
                           TextWrapping="Wrap" LineHeight="22"
                           IsTextSelectionEnabled="True"/>
            </ScrollViewer>
        </StackPanel>
    </Grid>
</Page>
